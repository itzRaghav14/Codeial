<header class="bg-blue-600 py-1 pl-3 pr-5">
    <nav class="flex justify-between text-white w-full">
        <h2 class="text-3xl font-semibold">codeial</h2>
        <ul class="flex gap-6 text-xl items-center font-medium">
            <% if(locals.user) { %>
                <li><a href="/users/profile/<%= user.id %>"><%= user.name %></a></li>
                <li><a href="/users/sign-out">Sign Out</a></li>
            <% } else { %>
                <li><a href="/users/sign-in">Sign In</a></li> / 
                <li><a href="/users/sign-up">Sign Up</a></li>
            <% } %>
        </ul>
    </nav>

</header>